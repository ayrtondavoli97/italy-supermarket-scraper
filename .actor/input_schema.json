{
  "title": "Italian Supermarket Flyers & Deals Scraper",
  "description": "Extract current promotional products from Italian supermarket flyers. Structured API records are returned by default; lower-quality preview fallback data can be optionally included.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "catena": {
      "title": "Supermarket chain",
      "type": "string",
      "description": "Select one chain or scrape all supported and candidate grocery chains.",
      "editor": "select",
      "enum": ["tutti", "conad", "lidl", "eurospin", "md", "aldi", "famila", "ins", "esselunga"],
      "enumTitles": ["All grocery chains", "Conad Superstore", "Lidl", "Eurospin", "MD Discount", "Aldi", "Famila (validation pending)", "iN's Mercato (validation pending)", "Esselunga (preview fallback only when available)"],
      "default": "tutti"
    },
    "keyword": {
      "title": "Product keyword filter (optional)",
      "type": "string",
      "description": "Return only products whose name or source category contains the term, for example: pasta, birra, latte, detersivo.",
      "editor": "textfield",
      "default": ""
    },
    "structuredOnly": {
      "title": "Return only structured API-quality records",
      "type": "boolean",
      "description": "Recommended. Excludes preview-only fallback records, such as currently available Esselunga preview items, from the output dataset.",
      "default": true
    },
    "maxItems": {
      "title": "Maximum products for a single selected chain",
      "type": "integer",
      "description": "Used only when you select a single supermarket chain.",
      "default": 1000,
      "minimum": 1,
      "maximum": 10000
    },
    "maxItemsPerChain": {
      "title": "Maximum products per chain when scraping all",
      "type": "integer",
      "description": "Used when Supermarket chain is set to All grocery chains. Prevents large chains from consuming the whole output before smaller chains are processed.",
      "default": 1000,
      "minimum": 1,
      "maximum": 10000
    },
    "maxTotalItems": {
      "title": "Maximum total products when scraping all",
      "type": "integer",
      "description": "Safety ceiling for an all-chains run. Keep this greater than the expected sum of products across chains for full coverage.",
      "default": 10000,
      "minimum": 1,
      "maximum": 50000
    },
    "diagnosticMode": {
      "title": "Save API diagnostics",
      "type": "boolean",
      "description": "Stores API response diagnostics for structured flyers. Use for development only.",
      "default": false
    },
    "investigateZeroResults": {
      "title": "Investigate chains without structured products",
      "type": "boolean",
      "description": "Development mode: for chains returning zero API products, saves page HTML, screenshot, DOM audit and click probes. Does not change extracted output.",
      "default": false
    },
    "proxyConfig": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Optional Apify Proxy configuration.",
      "editor": "proxy",
      "default": { "useApifyProxy": true }
    }
  }
}
